{
  "name": "stream-utils",
  "version": "0.0.0",
  "description": "utils for node.js streaming api ",
  "main": "index.js",
  "scripts": {
    "test": "mocha --recursive -R spec test",
    "coverage": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/airtoxin/stream-utils.git"
  },
  "keywords": [
    "stream"
  ],
  "author": "airtoxin",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/airtoxin/stream-utils/issues"
  },
  "dependencies": {
    "lodash": "*"
  },
  "devDependencies": {
    "mocha": "*",
    "stream-spec": "*",
    "istanbul": "*",
    "coveralls": "*",
    "mocha-lcov-reporter": "*"
  }
}
