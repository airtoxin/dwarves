{
  "name": "dwarves",
  "version": "0.0.1",
  "description": "util streams",
  "main": "index.js",
  "scripts": {
    "test": "mocha --recursive -R spec test",
    "coverage": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/airtoxin/dwarves.git"
  },
  "keywords": [
    "stream",
    "util"
  ],
  "author": "airtoxin",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/airtoxin/dwarves/issues"
  },
  "dependencies": {
    "lodash": "*"
  },
  "devDependencies": {
    "mocha": "*",
    "stream-spec": "*",
    "istanbul": "*",
    "coveralls": "*",
    "mocha-lcov-reporter": "*"
  }
}
